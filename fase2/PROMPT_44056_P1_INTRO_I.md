# 44056 ? parcial P1: Introducci?n y cap?tulo I

Trabaja solo en `fase2/corpus/intermedios/11362/44056/`. Lee `PREPARACION_FUENTE.md`, `indice_fuente.json`, `texto.txt` y `tramos/`.

Crea at?micamente `parcial_44056_p1.json`, una lista JSON que cubra `Introducci?n` y `I. Relaci?n entre el cambio climatico y las dimensiones de la seguridad alimentaria y nutricional`, con toda su descendencia literal del manifiesto.

Reglas:

- Conserva t?tulos, acentos, puntuaci?n, numeraci?n, niveles y orden literal; no reemplaces t?rminos extensos por siglas como SAN o GEI en el campo `seccion`.
- Cada nodo tiene `seccion`, `nivel`, `paginas`, `resumen`, `dimensiones` y `subsecciones`.
- Resume cada hoja desde su propio rango y usa citas literales autosuficientes con p?gina correcta para cada dimensi?n.
- No incluyas material editorial, listas de cuadros/gr?ficos, bibliograf?a, anexos ni encabezados corridos.
- No modifiques `borrador_preprueba.json`, otros parciales, el manifiesto ni resultados.
