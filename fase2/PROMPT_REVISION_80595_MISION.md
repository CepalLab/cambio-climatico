# Misión separada de revisión semántica — 11362/80595

Usa obligatoriamente `gpt-5.6-luna` con `-c model_reasoning_effort=xhigh`. Revisa exclusivamente `11362/80595`; no abras ni proceses otros IDs. Lee primero `PROMPT_REVISION_DIMENSIONES_CITAS.md` y aplica todas sus reglas. Usa únicamente `corpus/intermedios/11362/80595/tramos/`, `corpus/intermedios/11362/80595/indice_fuente.json` y `corpus/intermedios/11362/80595/borrador_preprueba.json`; no uses `texto.txt`, PDF, OCR, otra fuente, `corpus/resultados/` ni `ledger.py`.

Este documento requiere revisión completa porque supera 80 páginas, contiene títulos multilínea y layout con columnas. Recorre todas las dimensiones de `resumen_secciones` y las evidencias de interpelación, verificando en el marcador literal `=== PÁGINA PDF N ===`. Una cita debe ser una afirmación completa, aparecer en la página declarada y demostrar directamente el slug; rechaza fragmentos, rótulos, tablas/figuras, notas, encabezados, URLs o evidencia solo temática. Atención especial: la cita de página 41 sobre el canal de Panamá fue recortada durante la consolidación y debe sustituirse por la oración completa que figura en el tramo; verifica también el cierre normativo PDF 227–229 y que cada ítem del `como_hacerlo_concreto` provenga de su voz normativa propia.

Escribe atómicamente solo `corpus/intermedios/11362/80595/matriz_revision_dimensiones_citas.json`, como objeto con clave `entradas`. Cada entrada debe tener `ruta_seccion`, `dimension`, `pagina`, `veredicto` (`directa`, `parcial` o `no_sustenta`) y `justificacion`; las `parcial` requieren `decision_humana`, y ninguna `no_sustenta` puede quedar. Si una cita no sustenta, reemplázala por evidencia directa del mismo tramo, reclasifica o elimina la dimensión en `borrador_preprueba.json`; cualquier edición debe hacerse atómicamente y obliga a repetir después toda la ronda final de validaciones. No edites el índice ni los parciales. Comprueba al final el parseo y el aplanado depth-first si editaste el borrador. No ejecutes todavía los validadores ni el certificador: esa ronda la ejecutaré después de recibir tu matriz.
