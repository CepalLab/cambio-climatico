# Manifiestos de lotes

Cada ejecución de `ledger.py next-batch` crea un manifiesto `L####.json` con orden, handles, títulos y versión metodológica. Los manifiestos se versionan; el estado vivo permanece en SQLite.
