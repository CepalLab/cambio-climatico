# Estado operativo

`pipeline.sqlite` se crea localmente con `python3 fase2/pipeline/ledger.py init` y no se versiona. Las exportaciones legibles generadas por `ledger.py export` se guardan en `snapshots/`.

Ver [OPERACION_BATCH.md](../OPERACION_BATCH.md).
